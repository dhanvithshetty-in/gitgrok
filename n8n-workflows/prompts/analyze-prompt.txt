You are a senior software engineer analyzing a GitHub repository.

Repository: {{full_name}}
Description: {{description}}
Primary language (GitHub): {{language}}
Stars: {{stars}}
Forks: {{forks}}
Open Issues: {{open_issues}}
License: {{license}}

README content:
{{readme_content}}

package.json:
{{package_json}}

File structure ({{file_count}} files):
{{file_tree_json}}

File extension breakdown (by total bytes - this is the source of truth for the tech stack):
{{extension_breakdown}}

Analyze this repository and return ONLY valid JSON with this exact structure (no markdown, no code blocks). STRICT RULES:
1. summary.purpose: 1-2 specific sentences about what this project actually does, based on the README and file structure. Never write generic text like "Repository analysis completed."
2. summary.architecture: describe the REAL structure you can see (top-level directories, entry files, how parts connect). Never write generic text like "Standard web application architecture."
3. summary.quality: assess code organization and best practices shown by the file structure (1-2 sentences).
4. summary.healthScore: an integer between 0 and 100 estimating overall code health based on code quality, structure, and testing. summary.healthGrade: a single letter (A-F) that matches the healthScore (A=90-100, B=80-89, C=70-79, D=60-69, F=0-59).
5. techStack: derive ONLY from the file extension breakdown above. Never invent a language or framework whose extensions do not appear. Use category "language" for programming languages and "framework"/"tool" for frameworks and tools.
6. archDiagram: create a mermaid flowchart describing the REAL architecture from the top-level directories and key files. Use the syntax below but NEVER copy its content literally:
graph TD
    A[Component] --> B[Component]
7. stats: use EXACTLY the numbers provided above. Do not change, round, or invent them.

{
  "summary": {
    "purpose": "...",
    "architecture": "...",
    "quality": "...",
    "healthScore": 0,
    "healthGrade": "A",
    "strengths": ["List 3-5 key strengths of this codebase"],
    "recommendations": ["List 2-4 suggestions for improvement"]
  },
  "techStack": [
    { "name": "Language/Framework", "category": "language|framework|tool|database", "icon": "single emoji", "percentage": 45 }
  ],
  "archDiagram": "graph TD\n    A[Component] --> B[Component]",
  "stats": {
    "stars": 0,
    "forks": 0,
    "openIssues": 0,
    "license": "N/A",
    "contributors": 0,
    "lastCommit": ""
  }
}
